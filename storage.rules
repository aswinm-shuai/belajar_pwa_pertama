rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /warranty-claims/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if true;
    }
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
